# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# 虚拟环境（通过 setup_mac.sh 生成，体积约 1.2GB，不入库）
.venv/
venv/

# LaMa 模型权重（约 200MB，通过 download_model.sh 获取）
models/

# 运行日志
app.log

# 构建产物（通过 make_app.sh 重新生成）
MacWatermarkRemover.app/
Resources/AppIcon.icns

# 系统
.DS_Store
